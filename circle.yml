machine:
  node:
    version: 7.8.0

dependencies:
  override:
    - npm install
    - npm install webpack -g

test:
  override:
    - npm test
  post:
    - webpack
    - webpack -p --env production
    - npm run dist
    - mv zip/*.zip ${CIRCLE_ARTIFACTS}
